@model Quizler.Web.ViewModels.Quizzes.QuizCreateInputModel
@{
    ViewData["Title"] = "Create";
    var categories = Model.Categories.Select(c => new SelectListItem(c.Name, c.Id.ToString()));
}

<form method="post" asp-action="Create">
    @*<div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="form-group">
            <label asp-for="Name" class="control-label"></label>
            <input asp-for="Name" class="form-control" />
            <span asp-validation-for="Name" class="text-danger"></span>
        </div>
        <div class="row">
            <label asp-for="CategoryId"></label>
            <select asp-for="CategoryId" asp-items="categories"></select>
            <span asp-validation-for="CategoryId"></span>
        </div>
        <div class="form-group">
            <input type="submit" value="Create" class="btn btn-primary" />
        </div>*@
    <div class="content">
        <div class="login-div">
            <div class="row center-align">
                <h5>Create your quiz!</h5>
            </div>
            <div class="row">
                <div class="input-field col s12">
                    <label asp-for="Name" class="control-label"></label>
                    <input asp-for="Name" class="form-control" />
                    <span asp-validation-for="Name" class="text-danger"></span>
                </div>
            </div>
            <div class="row">
                    <label asp-for="CategoryId"></label>
                    <select asp-for="CategoryId" asp-items="categories"></select>
                    <span asp-validation-for="CategoryId"></span>
            </div>
            <div class="center-align">
                <button class="btn waves-effect waves-light orange" type="submit" name="action">
                    Create quiz
                </button>
            </div>
        </div>
    </div>
</form>

<br />
<br />
